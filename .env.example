# Neon PostgreSQL
DATABASE_URL=postgresql://user:password@region.neon.tech/dbname?sslmode=require

# Better Auth Cookie Secret (generate with: openssl rand -base64 32)
NEON_AUTH_COOKIE_SECRET=your_secret_here

# Next.js
NODE_ENV=development
